# AI Sales Agent (Shared Hosting Ready)

## Requirements
- PHP 8.1+
- MySQL 5.7+
- Apache with mod_rewrite

## Setup
1. Create MySQL database.
2. Import `database/migrations/schema.sql`.
3. Upload project into your hosting folder (root or subfolder).
4. Update DB settings in `config/config.php`.
5. Make sure `storage/uploads` is writable.
6. Open the folder URL directly (e.g. `https://domain.com/codex-sales-dashboard/`).

## Default Admin
- Email: `admin@aisalesagent.local`
- Password: `password123`
